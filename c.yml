#!/bin/bash

## ==========设置各参数（不需要的可以删掉或者前面加# ）==================
# 设置固定隧道参数 (如果设置，注意把前面的# 去掉，如果不设置，默认启用临时隧道)
# export TOK='xxxxx'
# export ARGO_DOMAIN='xxxxx'

#设置哪吒参数(NEZHA_TLS='1'开启tls,设置其他关闭tls)
export NEZHA_SERVER='nezha.biudpam.eu.org'
export NEZHA_KEY='NSFBwq4x9FfoISZ6NS'
export NEZHA_PORT='443'
export NEZHA_TLS='1'

# 下面设置UUID和路径，CF_IP是优选IP，SUB_NAME为节点名称
export UUID='eac8291b-7890-4944-9b63-564d7a680063'
export VPATH='vls'
export CF_IP='cdn.xn--b6gac.eu.org'
export SUB_NAME='deno'
#设置订阅上传地址
export SUB_URL='xxx'

# 设置启动玩具原程序，senver.jar 原启动文件改名
# export JAR_SH='java -Xms128M -XX:MaxRAMPercentage=95.0 -jar senver.jar'
# 默认启动原游戏后，不显示日志





